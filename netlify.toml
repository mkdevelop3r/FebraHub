[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# SPA: toda rota cai no index.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
