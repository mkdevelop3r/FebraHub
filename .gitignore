# NUNCA commitar credenciais.
# A service_role key do Supabase ignora toda a RLS —
# vazar ela é entregar o banco inteiro.
.env
.env.*
!.env.example
*.key
secrets/
# Chave de service account do Google (etl/service_account.json): tem
# private_key dentro, e quem tem ela lê as planilhas todas.
service_account*.json

#service
service_account.json
*service_account*.json

# Log do sync da agenda: cresce a cada rodada e nao diz nada
# que o banco ja nao saiba.
etl/agenda_sync.log
etl/agenda_sync.log.old

# Conversa de lead exportada para depuracao da auditoria. Tem contact_id,
# URL de audio e o que a pessoa falou. Commitar poe dado pessoal num
# historico do qual ele nao sai mais.
etl/transcricoes.csv
etl/auditorias_whatsapp.csv

# Node
node_modules/
dist/
.vite/

# Python
__pycache__/
*.pyc
.venv/
venv/

# SO
.DS_Store
Thumbs.db
